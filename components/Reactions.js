import {ButtonGroup} from "react-native-elements";
import React from "react";

export default ({id}) => {

  //TODO: Retrieve the Reactions list from API for Post ID

  return (
    <ButtonGroup
    />
  );
};
